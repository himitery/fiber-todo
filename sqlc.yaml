version: "2"

sql:
  - engine: "postgresql"
    queries: "sql/queries/*.sql"
    schema: "sql/schema.sql"
    gen:
      go:
        package: "sql"
        out: "internal/adapter/persistence/sql"
        sql_package: "pgx/v5"
